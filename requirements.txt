# Gemini Terminal - dependências de execução

# Google Gemini API
google-genai>=2.4.0

# Interface de terminal com formatação
rich>=13.0.0

# Histórico e autocomplete no terminal interativo
prompt-toolkit>=3.0.0

# Carregamento de variáveis de ambiente
python-dotenv>=1.0.0

# Acesso a banco de dados
sqlalchemy>=2.0.0

# Parsing de YAML para definição de processos
pyyaml>=6.0

# Driver SQL Server (necessário para as tools de SQL Server)
pyodbc>=5.0.0

# Manipulação e análise de dados
pandas>=2.0.0

# Conversão de DataFrames para tabelas em markdown
# necessário para as ferramentas de análise em tools/data_analysis.py
tabulate>=0.9.0

# Leitura de arquivos Excel
openpyxl>=3.0.0

# Leitura e extração de texto de arquivos PDF
pypdf>=5.0.0

# Geração de gráficos (plot_sheet_data / plot_table_data)
matplotlib>=3.7.0
plotext>=5.2.8

# Cliente HTTP (integração com a API do Airflow)
requests>=2.31.0

# Driver Hive (necessário para as tools de Hive via SQLAlchemy/LDAP)
pyhive[hive]>=0.7.0
thrift>=0.16.0

# Timeout das chamadas ao Gemini. Embora também seja dependência transitiva
# do google-genai, o projeto importa e trata exceções do httpx diretamente.
httpx>=0.27.0

# Testes
pytest>=7.4.0
